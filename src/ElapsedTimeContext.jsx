import { createContext, useContext, useDispatch } from 'react'

const ElapsedTimeContext = createContext(null);
const ElapsedTimeDispatchContext = createContext(null);

export default function ElapsedTimeContextProvider() {
    
} 